# Security and privacy hardening: examples

Examples are synthetic and non-authoritative.

## Contract or workflow example

Captured HTML is rendered only through the safe extracted projection; scripts and event handlers never reach a trusted dashboard context.

## Verification pattern

```bash
uv run pytest -q
uv run ruff check .
```

Record package-specific failure injection, parity, security, and recovery commands in the execution log.

## Evidence example

```text
Task: REL-SECURITY-001
Commit: <git-sha>
Scenario: <bounded failure or consistency case>
Command: <exact command>
Result: PASS
Canonical integrity: verified
Sensitive output: redacted
```
