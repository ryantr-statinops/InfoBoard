# Packaging and MVP release: examples

Examples are synthetic and non-authoritative.

## Contract or workflow example

A clean install starts with semantic provider unconfigured—not degraded—while local canonical and keyword workflows remain available.

## Verification pattern

```bash
uv sync --frozen
uv run ruff check .
uv run pytest -q
```

Add exact migration, browser, provider, recovery, and packaging commands during execution.

## Evidence example

```text
Task: RELEASE-PACKAGE-001
Commit: <git-sha>
Environment: <clean platform>
Command: <exact command>
Result: PASS
Artifacts: <redacted paths>
Reviewer: <name or handle>
```
