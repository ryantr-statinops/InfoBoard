# Collections, tags, notes, and bookmark state: examples

Examples are synthetic and non-authoritative; canonical contracts prevail.

## Contract or workflow example

Adding an existing tag membership with the current bookmark If-Match remains idempotent but returns the contractually required current/new version projection.

## Verification pattern

```bash
uv run pytest -q
uv run ruff check .
```

Add package-specific selectors, browser checks, and failure injection during delivery.

## Evidence example

```text
Task: ORG-CONTEXT-001
Commit: <git-sha>
Command: <exact command>
Result: PASS (<count and duration>)
Artifact: <report or screenshot path>
Deviation: none
```
