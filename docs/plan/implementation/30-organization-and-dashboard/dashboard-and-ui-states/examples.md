# Dashboard and UI states: examples

Examples are synthetic and non-authoritative; canonical contracts prevail.

## Contract or workflow example

A failed snapshot card keeps the bookmark visible, presents a safe reason and Retry action, and continues to show the last successful snapshot when one exists.

## Verification pattern

```bash
uv run pytest -q
uv run ruff check .
```

Add package-specific selectors, browser checks, and failure injection during delivery.

## Evidence example

```text
Task: ORG-UI-001
Commit: <git-sha>
Command: <exact command>
Result: PASS (<count and duration>)
Artifact: <report or screenshot path>
Deviation: none
```
