# Keyword query orchestration: examples

Examples are synthetic and non-authoritative; canonical contracts prevail.

## Contract or workflow example

A keyword request with two tag IDs uses OR within tags and AND with the selected collection and status; results contain one current projection per bookmark.

## Verification pattern

```bash
uv run pytest -q
uv run ruff check .
```

Add package-specific selectors, browser checks, and failure injection during delivery.

## Evidence example

```text
Task: RET-KEYWORD-001
Commit: <git-sha>
Command: <exact command>
Result: PASS (<count and duration>)
Artifact: <report or screenshot path>
Deviation: none
```
